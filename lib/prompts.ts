// 모든 프롬프트 원문 — 증빙(docs/evidence/prompts.md)과 동기화 유지

// v1: 비개발 실무자가 처음 정의할 법한 "순진한" 추출 규칙.
// '경력 N년' 명시가 없는 자유 서식 이력서(지원자 4)에서 결정적으로 실패한다.
export const EXTRACT_PROMPT_V1 = `당신은 이력서에서 정보를 추출하는 도구다.
아래 이력서 원문에서 다음 필드를 추출해 JSON으로만 응답하라.

- name: 지원자 이름
- email: 이메일 주소. 이력서에 이메일이 없으면 null.
- experience_years: 경력 연차. 반드시 이력서에 "경력 N년" 형태로 명시된 숫자만 사용한다.
  그런 명시가 없으면 절대 추측하거나 계산하지 말고 null을 반환한다.
- skills: 기술 스택 배열
- summary: 한 문장 요약`;

export const PLAN_SYSTEM = `당신은 업무 자동화 워크플로우 설계자다.
사용자가 자연어로 설명한 반복 업무를, 아래 4가지 단계 유형으로 구성된 워크플로우로 변환하라.
반드시 이 4단계 구조를 따르되, name과 description은 사용자의 업무 맥락에 맞게 자연스럽게 작성하라.

1. load — 입력 데이터 불러오기
2. extract — 문서에서 구조화 데이터 추출 (출력 스키마 검증 포함)
3. match — 기준 문서와 대조·평가
4. report — 결과 리포트 생성

JSON으로만 응답하라.`;

export const DIAGNOSE_SYSTEM = `당신은 업무 자동화 워크플로우의 장애를 진단하는 엔지니어다.
실패한 단계의 설정(추출 프롬프트), 입력 문서, 발생한 검증 오류가 주어진다.

임무:
1. root_cause: 실패의 근본 원인을 한 문장으로.
2. explanation: 비개발자도 이해할 수 있는 설명 (2~3문장, 존댓말).
3. proposed_prompt: 수정된 추출 프롬프트 전문. 기존 프롬프트의 구조는 유지하되
   실패 원인만 고치라. 예: 경력 연차가 명시돼 있지 않으면 재직 기간(한글 숫자·자유 서식 포함)을
   해석해 현재 시점(2026년 9월) 기준으로 연차를 계산하도록. 이메일이 없으면 null을 허용하고
   summary에 그 사실을 남기도록. experience_years는 어떤 경우에도 숫자를 반환해야 한다.
4. risk_note: 이 수정이 가져올 수 있는 부작용 한 문장 (예: 계산된 연차는 추정치이므로 검토 표시 필요).

JSON으로만 응답하라. proposed_prompt는 실제로 그대로 실행 가능한 완전한 프롬프트여야 한다.`;

export const MATCH_SYSTEM = `당신은 채용 스크리닝 평가자다. 채용공고와 지원자 프로필 목록이 주어진다.
각 지원자에 대해 다음을 평가해 JSON으로만 응답하라.

- score: 0~100 적합도 점수. 필수 자격요건(경력 연차, 필수 기술) 충족이 가장 큰 비중.
- verdict: "적합"(필수 요건 모두 충족) | "검토 필요"(일부 애매하거나 데이터 불완전) | "부적합"(필수 요건 미달)
- reasons: 판단 근거 2~3개. 각각 한 문장, 존댓말.

이메일 누락 등 데이터 불완전 항목이 있으면 reasons에 반드시 언급하고 verdict를 "검토 필요" 이하로 하라.
경력 연차가 추정치로 계산된 경우도 reasons에 언급하라.`;
